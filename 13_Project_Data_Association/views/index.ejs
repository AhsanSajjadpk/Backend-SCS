<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- /* Basic styling for the popup */ -->
    <style>
        .popup {
            display: none;
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            padding: 20px;
            background-color: white;
            border: 2px solid #f44336;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            z-index: 1000;
        }

        .popup.active {
            display: block;
        }

        .popup h2 {
            margin: 0;
            color: #f44336;
        }

        .popup p {
            margin: 10px 0;
        }

        .popup button {
            background-color: #f44336;
            color: white;
            padding: 10px;
            border: none;
            cursor: pointer;
        }

        .popup button:hover {
            background-color: #d32f2f;
        }
    </style>
</head>

<body>

    <div class="w-full min-h-screen bg-zinc-900 flex justify-center text-white p-10">

        <div class="box w-1/3 p-10  border-2 border-zinc-800">
            <div class="w-full flex justify-center">
                <h3 class="text-3xl mb-7 ">Create Account</h3>
            </div>
            <form action="/register" method="post">

                <input
                    class="px-3 mt-1 w-full block py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md "
                    type="text" placeholder="username" name="username">
                <input
                    class="px-3 mt-1 w-full block py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md "
                    type="text" placeholder="name" name="name">
                <input
                    class="px-3 mt-1 w-full block py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md "
                    type="number" placeholder="age" name="age">
                <input
                    class="px-3 mt-1 w-full block py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md "
                    type="email" placeholder="email" name="email">
                <input
                    class="px-3 mt-1 w-full block py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md "
                    type="password" placeholder="password" name="password">
                <input class="bg-blue-500 block w-full px-3 py-2 rounded-md mt-4" type="submit" value="Create Account">


            </form>
            <div class="w-full flex justify-center">
                <a class="text-sm mt-5 inline-block" href="/login">Already have an Account? <span
                        class="text-blue-500">Login here </span> </a>
            </div>
        </div>

    </div>

        <!-- sdf -->

        <!-- Popup structure -->
        <div id="userExistsPopup" class="popup">
            <h2>User Already Exists</h2>
            <p>The email you entered is already registered. Please use a different email or log in.</p>
            <button onclick="closePopup()">OK</button>
        </div>

    <script>

        // Show popup if userExists is true
        if (userExists == true) {
            document.getElementById('userExistsPopup').classList.add('active');
        }

        // Function to close the popup
        function closePopup() {
            document.getElementById('userExistsPopup').classList.remove('active');
        }

    </script>

    <!-- sdf -->
</body>

</html>