<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>

    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">

        <div class="w-full flex justify-end">
            <a class="px-3 py-2 rounded-md bg-red-500 inline-block " href="/logout">Logout</a>

        </div>


        <h3 class="text-3xl mb-3">Hello <%= data.name%> 👋</h3>
        <h5 class="mb-3">You can create a new post</h5>

        <form action="/post" method="post">

            <textarea placeholder="What's in your mind "
                class="block w-2/3 p-3 h-32 resize-none bg-transparent border-2 border-zinc-700 rounded-md"
                name="content"></textarea>
            <input class="block bg-blue-500 px-3 py-2 rounded-md mt-3" type="submit" value="Create Post">
        </form>

        <div class="posts mt-10">

            <h5>All posts</h5>

            <div class="postcontainer mt-5 flex flex-wrap justify-center gap-5 p-3">

                <% data.posts.reverse().forEach((post)=>{ %>

                    <div class="postdata w-1/3 overflow-hidden border-2 bg-zinc-800 border-zinc-700 p-3 rounded-md">

                        <h5 class="text-blue-500 text-2xl mb-2">@<%= data.username %></h5>
                        <p class="text-sm tracking-tight"><%= post.content %></p>
    
                        <small class="mt-3 inline-block"><%= post.likes.length %> like</small>
                        <div class="btns flex gap-10 mt-1 ">
    
                            <a class="text-blue-500" href="/like/<%= post._id %>" >
                                <%= post.likes.indexOf(data._id) === -1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-zinc-500" href="/edit/<%= post._id %>" >edit</a>
                            <a class="text-red-500" href="/delete/<%= post._id %>" >delete</a>
    
                        </div>
    
                    </div>


              <%  }) %>



            </div>

        </div>



    </div>
</body>

</html>