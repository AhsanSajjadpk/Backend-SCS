<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="w-full h-full bg-zinc-800">
    <div class=" p-5 color-white">

        <div class="nav mb-5">
            <a class="text-blue-600" href="/">Back to home</a>
        </div>
        <h1 class="text-3xl mb-5 tracking-tighter">All user</h1>

        <div class="users mt-2  w-full flex gap-5 flex-wrap py-2 px-5 justify-center rounded-lg">

            <% if(users.length> 0 ) {%>

                <% users.forEach((user)=>{ %>

                    <div class="card w-1/5 bg-zinc-600 p-3 rounded-lg overflow-hidden">

                        <div class="img w-full h-52 bg-red-500 rounded-lg">
                            <img class="h-full w-full object-cover object-top rounded-lg"
                                src="<%= user.image %>" alt="">
                        </div>

                        <h5 class="mt-3 text-3xl tracking-tighter">
                            <%= user.name %>
                        </h5>
                        <h3 class="text-white-900 mt-3 ">
                            <%= user.email%>
                        </h3>

                        <div class=" w-full flex justify-between mt-3">
                            <a class="text-yellow-600" href="/edit/<%= user._id%>">Edit</a>
                            <a class="text-red-600" href="/delete/<%= user._id%>">Delete</a>
                        </div>
                    </div>

                    <% }) %>


                <%} else { %>
                    <h1 class="text-zinc-500 text-3xl ">No user Yet</h1>

                    <%} %>


        </div>

    </div>
</body>

</html>